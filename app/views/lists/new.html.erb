<div class="container mt-5">
  <h1>Create a new list</h1>
  <%= simple_form_for @list do |f| %>
    <%= f.input :name, placeholder: 'give it a funky name' %>
      <%= f.input :photo, as: :file %>
    <%= f.submit class: 'btn btn-danger'%>
  <% end %>
</div>
