<div class="container">
  <h1>This is the bookmarks page</h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.submit "Add Bookmark" %>
  <% end %>
</div>
